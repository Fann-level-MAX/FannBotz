let handler = async m => {

let krtu = `SEWA`
m.reply(`
Absolutely _FREE_
Gak perlu sewa
Kalo need premium tag aja @${nomorown1.split`@`[0]}
`.trim()) // Tambah sendiri kalo mau
}
handler.command = /^(sewa(bot)?|premium)$/i

export default handler